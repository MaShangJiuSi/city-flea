<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.city.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user
        (openid, name, real_name, phone, sex, id_card, avatar, auth_status, user_status, balance, create_time)
        values (#{openid}, #{name}, #{realName}, #{phone}, #{sex}, #{idCard}, #{avatar}, #{authStatus}, #{userStatus}, #{balance}, #{createTime})
    </insert>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from user
        <where>
            <if test="begin != null">
                and create_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and create_time &lt;= #{end}
            </if>
        </where>
    </select>

</mapper>