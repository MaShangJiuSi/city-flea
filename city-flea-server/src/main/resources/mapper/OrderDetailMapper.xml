<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.city.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail
        (name, image, order_id, goods_id, price, `number`, amount)
        values
        <foreach collection="list" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.goodsId}, #{od.price}, #{od.number}, #{od.amount})
        </foreach>
    </insert>
</mapper>