<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.city.mapper.ShoppingCartMapper">
    <select id="list" resultType="com.city.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="goodsId != null">
                and goods_id = #{goodsId}
            </if>
            <if test="sellerId != null">
                and seller_id = #{sellerId}
            </if>
        </where>
        order by create_time desc
    </select>

    <insert id="insertBatch" parameterType="list">
        insert into shopping_cart
        (name, image, user_id, goods_id, seller_id, number, amount, create_time)
        values
        <foreach collection="list" item="sc" separator=",">
            (#{sc.name}, #{sc.image}, #{sc.userId}, #{sc.goodsId}, #{sc.sellerId}, #{sc.number}, #{sc.amount}, #{sc.createTime})
        </foreach>
    </insert>
</mapper>